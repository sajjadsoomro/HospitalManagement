<UserControl x:Class="HospitalManagement.App.Views.DoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HospitalManagement.App.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:dataModels="clr-namespace:HospitalManagement.Data.DataModels;assembly=HospitalManagement.Data"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="DoctorTemplate" DataType="{x:Type dataModels:DoctorModel}">
            <materialDesign:Flipper materialDesign:ElevationAssist.Elevation="Dp0" Margin="20">
                <!-- Front Content -->
                <materialDesign:Flipper.FrontContent>
                    <Grid Width="200" Height="256">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="160" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <materialDesign:ColorZone VerticalAlignment="Stretch" Mode="PrimaryLight">
                            <materialDesign:PackIcon
                                Width="128"
                                Height="128"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Kind="AccountCircle" />
                        </materialDesign:ColorZone>
                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="{Binding DoctorName}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Specialization}" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                            <Button
                                Margin="0,4,0,0"
                                Command="{x:Static materialDesign:Flipper.FlipCommand}"
                                Content="DETAILS"
                                Style="{StaticResource MaterialDesignFlatButton}" />
                        </StackPanel>
                    </Grid>
                </materialDesign:Flipper.FrontContent>

                <!-- Back Content -->
                <materialDesign:Flipper.BackContent>
                    <Grid Width="200" Height="256">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <materialDesign:ColorZone Padding="6" Mode="SecondaryMid">
                            <StackPanel Orientation="Horizontal">
                                <Button
                                    HorizontalAlignment="Left"
                                    Command="{x:Static materialDesign:Flipper.FlipCommand}"
                                    Style="{StaticResource MaterialDesignToolForegroundButton}">
                                    <materialDesign:PackIcon HorizontalAlignment="Right" Kind="ArrowLeft" />
                                </Button>
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="DETAILS" />
                            </StackPanel>
                        </materialDesign:ColorZone>
                        <Grid
                            Grid.Row="1"
                            Width="172"
                            Margin="0,6,0,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <TextBox
                                Margin="0,12,0,0"
                                materialDesign:HintAssist.Hint="Full Name"
                                materialDesign:HintAssist.IsFloating="True"
                                Text="{Binding DoctorName}" />
                            <TextBox
                                Grid.Row="1"
                                Margin="0,12,0,0"
                                materialDesign:HintAssist.Hint="Specialization"
                                materialDesign:HintAssist.IsFloating="True"
                                Text="{Binding Specialization}" />
                            <TextBox
                                Grid.Row="2"
                                Margin="0,12,0,0"
                                materialDesign:HintAssist.Hint="Phone Number"
                                materialDesign:HintAssist.IsFloating="True"
                                Text="{Binding PhoneNumber}" />
                            <TextBox
                                Grid.Row="3"
                                Margin="0,12,0,0"
                                materialDesign:HintAssist.Hint="Specialty"
                                materialDesign:HintAssist.IsFloating="True"
                                Text="{Binding Email}" />
                        </Grid>
                    </Grid>
                </materialDesign:Flipper.BackContent>
            </materialDesign:Flipper>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <ItemsControl  ItemsSource="{Binding Doctors}" ItemTemplate="{StaticResource DoctorTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
