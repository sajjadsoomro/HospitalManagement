<UserControl x:Class="HospitalManagement.App.Views.PatientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HospitalManagement.App.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border>
            <DockPanel>
                <TextBlock Margin="10,0" Text="List of Patients" FontSize="30" FontWeight="Regular"
                           VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <Button HorizontalAlignment="Right" Margin="10,0"
                        Command="{Binding EditPatientInfoCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon
                            Margin="0,.5,4,0"
                            Kind="Add" />
                        <TextBlock
                            Text="Add Patient" />
                    </StackPanel>
                </Button>
            </DockPanel>
            
        </Border>
        <DataGrid Grid.Row="1"
            Margin="10"
            IsReadOnly="True"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            ItemsSource="{Binding Patients}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding FirstName}" Header="First Name"/>
                <DataGridTextColumn Binding="{Binding LastName}" Header="Last Name"/>
                <DataGridTextColumn Binding="{Binding DateOfBirth, StringFormat=dd/MM/yyyy}" Header="Date Of Birth"/>
                <DataGridTextColumn Binding="{Binding Gender}" Header="Gender"/>
                <DataGridTextColumn Binding="{Binding PhoneNumber}" Header="Phone Number"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{materialDesign:PackIcon Pencil}"
                                    Command="{Binding DataContext.EditPatientInfoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding Id}"/>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.Header>
                        <TextBlock Text="Edit" HorizontalAlignment="Center"></TextBlock>
                    </DataGridTemplateColumn.Header>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{materialDesign:PackIcon PlusThick}" Width="50" HorizontalAlignment="Left"
                                    Command="{Binding DataContext.AddAppointmentInfoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding Id}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.Header>
                        <TextBlock Margin="10,0,0,0" Text="Add Appointment" HorizontalAlignment="Right"></TextBlock>
                    </DataGridTemplateColumn.Header>
                </DataGridTemplateColumn>


            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
